#include <iostream>
using namespace std;

int main(){
    //review math operator:
    // % remainder
    // +, -, *, / 
    
    //assignment operator
    // = += -= *= /= assign value from right to left
    
    //new
    //relational operator (goal: determine true or false) 
    //true or false can be represented in many ways
    //integer: 1 mean true, 0 means false
    //boolean: true or false
    
    // > greater than 
    // < less than 
    // >= greater than or equal to 
    // <= less than or equal to
    // == equal 
    // != not equal
    int x;
    x=(8!=10);
    cout<<x;
    
    return 0;
}